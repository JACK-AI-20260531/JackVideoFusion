# v2.1 M1 工作流模板化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付任务级混剪参数模板 + 全自动步骤管线(素材分割→随机混剪→自动发布)+ 定时跑管线(PRD-v2.1 FR-1/2/3)。

**Architecture:** 两个新服务域:`mix-template`(参数模板存取)与 `pipeline`(管线校验/编排/调度),均为"纯函数 + 依赖注入"模式(照 schedule-store);管线运行 = 一个 taskQueue 任务(`type: 'pipeline'`),runner 串行执行注入的步骤执行器,产物链式传递,失败即停。IPC 走 `safeHandle` + `{ok,data,error}`。

**Tech Stack:** Electron 32 主进程 TS、node:test + `assert/strict`(import 带 `.ts` 后缀)、Vue 3 setup、零新增运行时依赖。

## Global Constraints

- 门禁三连全绿才算完成:`npm test`(当前 825+ 用例)+ `npm run typecheck` + `npx tsc -p tsconfig.electron.json --noEmit`
- 注释:中文 JSDoc,文件头写"职责/设计要点",不写魔法数字(阈值集中导出常量)
- 纯函数不 import electron;I/O 用 load/persist 注入(参考 `schedule-store.ts`)
- IPC:`safeHandle(ipc, 'channel', handler)`,入参逐字段校验后抛中文错误
- 新 IPC 模块必须在 `electron/ipc/index.ts` 的 `registerAllIpc` registrars 数组追加
- 渲染层 IPC 传参 `JSON.parse(JSON.stringify(...))` 深拷贝
- 单测文件放 `__tests__/*.spec.ts`,运行单个用例:`node --test --import tsx <spec路径>`
- 提交信息风格:`feat(pipeline): ...` / `feat(mix-template): ...`;每任务一提交,**禁止** `git add -A`(防误加 models/)

## File Structure

```
src/shared/types.ts                                  # Modify: TaskType += 'pipeline'
src/main/services/mix-template/
  types.ts                                           # MixTemplate/MixTemplateMeta
  template-store.ts                                  # MixTemplateStore + validateTemplateInput + 单例
  __tests__/template-store.spec.ts
src/main/ipc/mix-template.ipc.ts                     # mix-template:save/list/load/delete
src/main/services/pipeline/
  types.ts                                           # Pipeline/Step/Schedule/RunState + 三类步骤参数
  validate.ts                                        # validateSteps/validateSchedule 纯函数
  store.ts                                           # PipelineStore(pipelines.json)
  runner.ts                                          # StepExecutor 接口 + runPipeline 串行编排
  scheduler.ts                                       # isPipelineDue 纯函数 + PipelineScheduler 轮询
  index.ts                                           # 单例 wiring:默认执行器(ffmpeg/videoMix/publishQueue)
  __tests__/validate.spec.ts
  __tests__/runner.spec.ts
  __tests__/scheduler.spec.ts
src/main/ipc/pipeline.ipc.ts                         # pipeline:save/list/get/delete/run/cancelRun
src/renderer/views/video-mix/MixTemplateBar.vue      # 模板条(存为模板/下拉/删除)
src/renderer/views/video-mix/useMixTemplates.ts      # 模板 IPC 调用 composable
src/renderer/views/pipeline/PipelineView.vue         # 流水线编排页
src/renderer/router/index.ts                         # Modify: /pipeline 路由
src/renderer/components/layout/Sidebar.vue           # Modify: 导航项
electron/ipc/index.ts                                # Modify: 注册 2 个新 IPC 模块
docs/操作手册.md / docs/全流程操作指南.md / AGENTS.md   # Modify: 文档同步
```

---

### Task 1: mix-template 服务(types + store + 校验)

**Files:**
- Create: `src/main/services/mix-template/types.ts`
- Create: `src/main/services/mix-template/template-store.ts`
- Test: `src/main/services/mix-template/__tests__/template-store.spec.ts`

**Interfaces:**
- Consumes: `MixParams`(来自 `../video-mix/types`,已存在)
- Produces: `MixTemplate{id,name,description?,params,createdAt,updatedAt}`、`MixTemplateMeta`、`MixTemplateStore`(方法 `save/get/listMeta/remove`)、`validateTemplateInput(name, params): string | null`、单例 `mixTemplateStore`

- [ ] **Step 1: 写失败测试**

`src/main/services/mix-template/__tests__/template-store.spec.ts`:

```ts
/**
 * 混剪参数模板存储单测
 * 运行:node --test --import tsx src/main/services/mix-template/__tests__/template-store.spec.ts
 */
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { MixTemplateStore, validateTemplateInput } from '../template-store.ts';
import type { MixParams } from '../../video-mix/types.ts';
import type { MixTemplate } from '../types.ts';

/** 最小合法 MixParams(供各用例复用) */
function validParams() {
  return {
    mode: 'random' as const,
    folderIds: ['f1', 'f2'],
    perFolderCount: 3,
    resolution: '1080p' as const,
    keepOriginalQuality: false,
  };
}

/** 内存持久化工厂(load/persist 注入,绕开 electron) */
function memoryStore() {
  const saved: { data: MixTemplate[] } = { data: [] };
  const store = new MixTemplateStore({
    load: () => JSON.parse(JSON.stringify(saved.data)),
    persist: (list) => {
      saved.data = JSON.parse(JSON.stringify(list));
    },
  });
  return { store, saved };
}

describe('validateTemplateInput', () => {
  test('合法输入返回 null', () => {
    assert.equal(validateTemplateInput('剧情号', validParams()), null);
  });

  test('空名称/超长名称/非法 mode/空 folderIds 报错', () => {
    assert.ok(validateTemplateInput('', validParams()));
    assert.ok(validateTemplateInput('x'.repeat(51), validParams()));
    assert.ok(validateTemplateInput('t', { ...validParams(), mode: 'bad' as never }));
    assert.ok(validateTemplateInput('t', { ...validParams(), folderIds: [] }));
    assert.ok(validateTemplateInput('t', undefined as never));
  });
});

describe('MixTemplateStore', () => {
  test('save 后 get/listMeta 可取回,同名覆盖且 id 稳定', () => {
    const { store } = memoryStore();
    const first = store.save('剧情号', validParams(), '描述A');
    assert.equal(first.name, '剧情号');
    assert.equal(store.get('剧情号')?.params.folderIds.length, 2);

    // 同名覆盖:id 不变,updatedAt 刷新
    const again = store.save('剧情号', { ...validParams(), perFolderCount: 5 });
    assert.equal(again.id, first.id);
    assert.equal(again.params.perFolderCount, 5);
    assert.equal(store.listMeta().length, 1);
  });

  test('listMeta 按更新时间降序,且不含 params', () => {
    const { store } = memoryStore();
    store.save('a', validParams());
    store.save('b', validParams());
    const metas = store.listMeta();
    assert.equal(metas.length, 2);
    assert.equal(metas[0].name, 'b'); // 后保存的在前
    assert.equal('params' in metas[0], false);
  });

  test('remove 删除成功返回 true,不存在返回 false', () => {
    const { store } = memoryStore();
    store.save('a', validParams());
    assert.equal(store.remove('a'), true);
    assert.equal(store.get('a'), null);
    assert.equal(store.remove('a'), false);
  });

  test('重启恢复:load 注入的数据懒加载后可见', () => {
    const { store, saved } = memoryStore();
    store.save('a', validParams());

    const store2 = new MixTemplateStore({ load: () => saved.data, persist: () => {} });
    assert.ok(store2.get('a'));
  });
});
```

注意:`memoryStore` 里的 `type MixTemplateStoreDeps` 声明放在工厂函数之前(测试文件顶部 import 之后),避免 `use before declare` 告警;若 lint 报顺序问题,直接内联 `{ load: () => [], persist: () => {} }`。

- [ ] **Step 2: 运行确认失败**

Run: `node --test --import tsx src/main/services/mix-template/__tests__/template-store.spec.ts`
Expected: FAIL(模块不存在)

- [ ] **Step 3: 最小实现**

`src/main/services/mix-template/types.ts`:

```ts
/**
 * 混剪参数模板服务 - 类型定义
 * 职责:任务级 MixParams 快照的模板结构(PRD-v2.1 FR-1)
 * 与 config-service 的 ConfigTemplate(全局配置快照)互不影响
 */
import type { MixParams } from '../video-mix/types';

/** 混剪参数模板(按 name 唯一) */
export interface MixTemplate {
  /** 模板 ID(保存时生成,同名覆盖不变) */
  id: string;
  /** 模板名称(唯一标识) */
  name: string;
  /** 模板描述(可选) */
  description?: string;
  /** 混剪参数快照 */
  params: MixParams;
  /** 创建时间(ISO 8601) */
  createdAt: string;
  /** 最后更新时间(ISO 8601) */
  updatedAt: string;
}

/** 模板元数据(列表展示用,不含 params) */
export interface MixTemplateMeta {
  id: string;
  name: string;
  description?: string;
  createdAt: string;
  updatedAt: string;
}
```

`src/main/services/mix-template/template-store.ts`:

```ts
/**
 * 混剪参数模板存储(PRD-v2.1 FR-1)
 * 职责:MixParams 命名快照的保存/加载/删除,落 userData/mix-templates/templates.json
 * 设计要点:
 *  - 依赖注入 load/persist(默认 electron fs 实现),单测绕开 electron
 *  - 同名覆盖(upsert),id 稳定;每次变更即落盘
 */
import { app } from 'electron';
import { join, dirname } from 'path';
import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'fs';
import { logger } from '../../utils/logger';
import type { MixParams } from '../video-mix/types';
import type { MixTemplate, MixTemplateMeta } from './types';

/** 模板名最大长度 */
export const TEMPLATE_NAME_MAX_LEN = 50;

/**
 * 校验模板输入
 * @returns 错误信息;合法返回 null
 */
export function validateTemplateInput(name: string, params: MixParams): string | null {
  if (!name || name.trim().length === 0) return '模板名称不能为空';
  if (name.length > TEMPLATE_NAME_MAX_LEN) return `模板名称过长(≤${TEMPLATE_NAME_MAX_LEN} 字符)`;
  if (!params || typeof params !== 'object') return 'params 缺失';
  if (params.mode !== 'random' && params.mode !== 'audio-match') return 'params.mode 非法';
  if (!Array.isArray(params.folderIds) || params.folderIds.length === 0) {
    return 'folderIds 不能为空';
  }
  return null;
}

/** 依赖注入(单测注入内存实现) */
export interface MixTemplateStoreDeps {
  load?: () => MixTemplate[];
  persist?: (templates: MixTemplate[]) => void;
}

/** 默认持久化文件路径(userData/mix-templates/templates.json) */
function storeFile(): string {
  const dir = join(app.getPath('userData'), 'mix-templates');
  mkdirSync(dir, { recursive: true });
  return join(dir, 'templates.json');
}

function defaultLoad(): MixTemplate[] {
  try {
    const fp = storeFile();
    if (!existsSync(fp)) return [];
    return JSON.parse(readFileSync(fp, 'utf8')) as MixTemplate[];
  } catch (err) {
    logger.error(`[mix-template] 加载失败: ${err instanceof Error ? err.message : String(err)}`);
    return [];
  }
}

function defaultPersist(templates: MixTemplate[]): void {
  try {
    const fp = join(app.getPath('userData'), 'mix-templates', 'templates.json');
    mkdirSync(dirname(fp), { recursive: true });
    writeFileSync(fp, JSON.stringify(templates, null, 2), 'utf8');
  } catch (err) {
    logger.error(`[mix-template] 持久化失败: ${err instanceof Error ? err.message : String(err)}`);
  }
}

/** 混剪参数模板存储(按 name 索引,内存 Map + 每次变更落盘) */
export class MixTemplateStore {
  private templates = new Map<string, MixTemplate>();
  private readonly loadFn: () => MixTemplate[];
  private readonly persistFn: (list: MixTemplate[]) => void;
  private loaded = false;

  constructor(deps: MixTemplateStoreDeps = {}) {
    this.loadFn = deps.load ?? defaultLoad;
    this.persistFn = deps.persist ?? defaultPersist;
  }

  private ensureLoaded(): void {
    if (this.loaded) return;
    for (const t of this.loadFn()) this.templates.set(t.name, t);
    this.loaded = true;
  }

  private flush(): void {
    this.persistFn(this.listFull());
  }

  /** 保存(同名覆盖,id 稳定) */
  save(name: string, params: MixParams, description?: string): MixTemplate {
    const err = validateTemplateInput(name, params);
    if (err) throw new Error(`mix-template:save 参数无效:${err}`);
    const existing = this.templates.get(name);
    const now = new Date().toISOString();
    const tpl: MixTemplate = {
      id: existing?.id ?? `mixtpl-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`,
      name,
      description: description || undefined,
      params: JSON.parse(JSON.stringify(params)) as MixParams,
      createdAt: existing?.createdAt ?? now,
      updatedAt: now,
    };
    this.ensureLoaded();
    this.templates.set(name, tpl);
    this.flush();
    return tpl;
  }

  /** 按名称取模板 */
  get(name: string): MixTemplate | null {
    this.ensureLoaded();
    return this.templates.get(name) ?? null;
  }

  /** 元数据列表(按更新时间降序) */
  listMeta(): MixTemplateMeta[] {
    return this.listFull().map(({ params: _params, ...meta }) => meta);
  }

  /** 删除,返回是否删除成功 */
  remove(name: string): boolean {
    this.ensureLoaded();
    const ok = this.templates.delete(name);
    if (ok) this.flush();
    return ok;
  }

  private listFull(): MixTemplate[] {
    this.ensureLoaded();
    return [...this.templates.values()].sort((a, b) => b.updatedAt.localeCompare(a.updatedAt));
  }
}
// 注意:save/get 中 ensureLoaded 需在写入 Map 前调用(先读后写,避免覆盖已持久化数据)

/** 全局单例 */
export const mixTemplateStore = new MixTemplateStore();
```

实现要点:`flush()` 只保留一个,调用 `this.persistFn(this.listFull())`;`save` 中 `ensureLoaded()` 必须在 `this.templates.set` 之前调用。

- [ ] **Step 4: 运行确认通过**

Run: `node --test --import tsx src/main/services/mix-template/__tests__/template-store.spec.ts`
Expected: PASS(全部用例)

- [ ] **Step 5: 提交**

```powershell
git add src/main/services/mix-template
git commit -m "feat(mix-template): 任务级混剪参数模板存储(PRD-v2.1 FR-1)"
```

---

### Task 2: mix-template IPC 注册

**Files:**
- Create: `src/main/ipc/mix-template.ipc.ts`
- Modify: `electron/ipc/index.ts`(import + registrars 数组)

**Interfaces:**
- Consumes: Task 1 的 `mixTemplateStore`(`save/get/listMeta/remove`)
- Produces: 4 通道 `mix-template:save / list / load / delete`(渲染层 `window.api.invoke` 调用,返回 `{ok,data,error}`)

- [ ] **Step 1: 实现 IPC 模块**

`src/main/ipc/mix-template.ipc.ts`:

```ts
/**
 * 混剪参数模板 IPC 注册(PRD-v2.1 FR-1)
 * 通道列表:
 *   mix-template:save   - 保存(同名覆盖)
 *   mix-template:list   - 元数据列表(不含 params)
 *   mix-template:load   - 按名称取完整模板
 *   mix-template:delete - 按名称删除
 */
import type { ipcMain } from 'electron';
import { safeHandle } from '../../../electron/ipc/index';
import { mixTemplateStore, validateTemplateInput } from '../services/mix-template/template-store';
import type { MixParams } from '../services/video-mix/types';

interface SavePayload {
  name: string;
  params: MixParams;
  description?: string;
}

export function register(ipc: typeof ipcMain): void {
  safeHandle(ipc, 'mix-template:save', async (_event, payload) => {
    const p = payload as { name?: string; params?: MixParams; description?: string } | undefined;
    if (!p?.name || !p.params) throw new Error('mix-template:save 参数无效:缺少 name/params');
    const err = validateTemplateInput(p.name, p.params);
    if (err) throw new Error(`mix-template:save 参数无效:${err}`);
    return mixTemplateStore.save(p.name, JSON.parse(JSON.stringify(p.params)), p.description);
  });

  safeHandle(ipc, 'mix-template:list', async () => {
    return mixTemplateStore.listMeta();
  });

  safeHandle(ipc, 'mix-template:load', async (_event, payload) => {
    const p = payload as { name?: string } | undefined;
    if (!p?.name) throw new Error('mix-template:load 参数无效:缺少 name');
    return mixTemplateStore.get(p.name);
  });

  safeHandle(ipc, 'mix-template:delete', async (_event, payload) => {
    const p = payload as { name?: string } | undefined;
    if (!p?.name) throw new Error('mix-template:delete 参数无效:缺少 name');
    return mixTemplateStore.remove(p.name);
  });
}
```

- [ ] **Step 2: 注册到 registerAllIpc**

修改 `electron/ipc/index.ts`:

```ts
// import 区(在 registerTextTimeline 之后):
import { register as registerMixTemplate } from '../../src/main/ipc/mix-template.ipc';

// registrars 数组末尾(registerTextTimeline 之后)追加:
    registerMixTemplate,     // 混剪参数模板(PRD-v2.1 FR-1)
```

- [ ] **Step 3: 门禁验证**

Run: `npm test`
Expected: PASS(825+ 用例,含 Task 1 新增)

Run: `npm run typecheck`
Expected: PASS

Run: `npx tsc -p tsconfig.electron.json --noEmit`
Expected: PASS

- [ ] **Step 4: 提交**

```powershell
git add src/main/ipc/mix-template.ipc.ts electron/ipc/index.ts
git commit -m "feat(mix-template): IPC 通道 save/list/load/delete"
```

---

### Task 3: 随机混剪页模板条 UI

**Files:**
- Create: `src/renderer/views/video-mix/useMixTemplates.ts`
- Create: `src/renderer/views/video-mix/MixTemplateBar.vue`
- Modify: `src/renderer/views/video-mix/RandomMixTab.vue`

**Interfaces:**
- Consumes: Task 2 的 4 个 IPC 通道
- Produces: `useMixTemplates()` composable(`templates/list/save/remove` + `loading`);`MixTemplateBar` 组件 props/emit(见下)

- [ ] **Step 1: composable**

`src/renderer/views/video-mix/useMixTemplates.ts`:

```ts
/**
 * 混剪参数模板 composable
 * 职责:封装 mix-template:* IPC 调用,供 MixTemplateBar 与宿主 Tab 共用
 */
import { ref } from 'vue';
import type { MixParams } from './useMixActions';

/** 主进程 MixTemplate 结构(与 main/services/mix-template/types.ts 对齐) */
export interface MixTemplateMeta {
  id: string;
  name: string;
  description?: string;
  createdAt: string;
  updatedAt: string;
}

interface IpcResp<T> {
  ok: boolean;
  data?: T;
  error?: string;
}

interface WindowApi {
  invoke: <TReq, TResp>(channel: string, payload?: TReq) => Promise<IpcResp<TResp>>;
}

function getApi(): WindowApi {
  return (window as unknown as { api: WindowApi }).api;
}
```

继续同一文件的剩余部分:

```ts
/** 加载模板元数据列表 */
/** 加载模板元数据列表 */
export function useMixTemplates() {
  const templates = ref<MixTemplateMeta[]>([]);
  const loading = ref(false);

  async function refresh(): Promise<void> {
    const res = await getApi().invoke<undefined, MixTemplateMeta[]>('mix-template:list');
    if (res.ok && res.data) templates.value = res.data;
  }

  async function save(name: string, params: MixParams, description?: string): Promise<string | null> {
    const res = await getApi().invoke<{ name: string; params: MixParams; description?: string }, MixTemplateMeta>(
      'mix-template:save',
      JSON.parse(JSON.stringify({ name, params, description })),
    );
    if (!res.ok) return res.error ?? '保存失败';
    await refresh();
    return null;
  }

  async function load(name: string): Promise<MixParams | null> {
    const res = await getApi().invoke<{ name: string }, { params: MixParams }>('mix-template:load', { name });
    if (!res.ok || !res.data) return null;
    return JSON.parse(JSON.stringify(res.data.params)) as MixParams;
  }

  async function remove(name: string): Promise<void> {
    await getApi().invoke<{ name: string }, boolean>('mix-template:delete', { name });
    await refresh();
  }

  return { templates, loading, refresh, save, load, remove };
}
```

- [ ] **Step 2: MixTemplateBar 组件**

`src/renderer/views/video-mix/MixTemplateBar.vue`:

```vue
<script setup lang="ts">
/**
 * 混剪参数模板条(PRD-v2.1 FR-1)
 * 职责:模板下拉选择 + 套用 + 存为模板 + 删除
 * props.buildParams 由宿主提供(从表单组装快照);套用时 emit('apply') 由宿主写回表单
 */
import { ref, onMounted } from 'vue';
import { useMixTemplates } from './useMixTemplates';
import type { MixParams } from './useMixActions';

const props = defineProps<{ buildParams: () => MixParams }>();
const emit = defineEmits<{
  (e: 'apply', params: MixParams): void;
}>();

const { templates, refresh, save, load, remove } = useMixTemplates();
const selectedName = ref('');
const newName = ref('');
const message = ref('');
const messageType = ref<'ok' | 'err'>('ok');

onMounted(() => {
  void refresh();
});

async function handleApply(): Promise<void> {
  if (!selectedName.value) return;
  const params = await load(selectedName.value);
  if (params) emit('apply', params);
}

async function handleSave(): Promise<void> {
  const name = newName.value.trim();
  if (!name) {
    message.value = '请填写模板名称';
    messageType.value = 'err';
    return;
  }
  const snapshot = props.buildParams();
  const err = await save(name, snapshot);
  message.value = err ? `保存失败:${err}` : `已保存模板「${name}」`;
  messageType.value = err ? 'err' : 'ok';
  newName.value = '';
}

async function handleDelete(): Promise<void> {
  if (!selectedName.value) return;
  await remove(selectedName.value);
  selectedName.value = '';
  message.value = '已删除';
  messageType.value = 'ok';
}
</script>

<template>
  <div class="tpl-bar">
    <select v-model="selectedName" :disabled="!templates.length" @change="handleApply">
      <option value="" disabled>选择模板…</option>
      <option v-for="t in templates" :key="t.id" :value="t.name">{{ t.name }}</option>
    </select>
    <button class="btn" :disabled="!selectedName" @click="handleApply">套用</button>
    <button class="btn danger" :disabled="!selectedName" @click="handleDelete">删除</button>
    <input v-model="newName" placeholder="新模板名称" class="tpl-name" />
    <button class="btn primary" @click="handleSave">📌 存为模板</button>
    <span v-if="message" :class="['msg', messageType]">{{ message }}</span>
  </div>
</template>

<style scoped>
.tpl-bar { display: flex; gap: 8px; align-items: center; margin-bottom: 12px; }
.tpl-name { width: 160px; }
.msg.ok { color: #2e9e5b; }
.msg.err { color: #d9534f; }
</style>
```

组件 API 锁定:props `{ buildParams: () => MixParams }`,emits `apply(params)`。

- [ ] **Step 3: 宿主接线(RandomMixTab.vue)**

在 `RandomMixTab.vue` 顶部 template 区(第一个 `<section class="form-section">` 之前)插入:

```vue
<MixTemplateBar :build-params="buildParamsSnapshot" @apply="applyTemplate" />
```

script setup 中追加(状态名与现有 refs 一一对应,均已核实):

```ts
import MixTemplateBar from './MixTemplateBar.vue';

/** 当前表单 → MixParams 快照(与 handleStart 的组装逻辑一致) */
function buildParamsSnapshot(): MixParams {
  return {
    mode: 'random',
    folderIds: selectedFolderIds.value.slice(),
    perFolderCount: perFolderCount.value,
    targetDurationSec: targetDurationSec.value,
    uniqueReuse: uniqueReuse.value,
    skipRecentUsed: skipRecentUsed.value,
    brandKit: brandKit.value,
    segmentSec: segmentSec.value,
    resolution: resolution.value,
    keepOriginalQuality: keepOriginalQuality.value,
    transitionSec: transitionSec.value,
    watermark: watermarkConfig.value.enabled ? watermarkConfig.value : null,
    subtitle: subtitleEnabled.value && subtitleSrtPath.value
      ? { srtPath: subtitleSrtPath.value, style: subtitleStyle.value }
      : null,
    outputDir: outputDir.value,
    outputName: outputName.value || `random-mix-${Date.now()}.mp4`,
  };
}

/** 模板 → 写回全部表单 ref */
function applyTemplate(p: MixParams): void {
  selectedFolderIds.value = (p.folderIds ?? []).slice();
  perFolderCount.value = p.perFolderCount ?? 3;
  targetDurationSec.value = p.targetDurationSec ?? 0;
  uniqueReuse.value = p.uniqueReuse ?? true;
  skipRecentUsed.value = p.skipRecentUsed ?? false;
  brandKit.value = p.brandKit ?? false;
  segmentSec.value = p.segmentSec ?? 5;
  resolution.value = p.resolution ?? '1080p';
  keepOriginalQuality.value = p.keepOriginalQuality ?? false;
  transitionSec.value = p.transitionSec ?? 0;
  watermarkConfig.value = p.watermark
    ? { ...p.watermark }
    : { ...watermarkConfig.value, enabled: false };
  subtitleEnabled.value = !!p.subtitle;
  subtitleSrtPath.value = p.subtitle?.srtPath ?? '';
  if (p.subtitle?.style) subtitleStyle.value = { ...p.subtitle.style };
  outputDir.value = p.outputDir ?? outputDir.value;
  outputName.value = p.outputName ?? '';
}
```

`<template>` 最外层 `<div class="mix-tab">` 的第一个子元素位置放 `<MixTemplateBar ... />`。

- [ ] **Step 4: 门禁验证**

Run: `npm run typecheck`
Expected: PASS

- [ ] **Step 5: 提交**

```powershell
git add src/renderer/views/video-mix
git commit -m "feat(mix-template): 随机混剪页模板条(存为模板/套用/删除)"
```

---

### Task 4: pipeline 类型 + 校验 + 存储

**Files:**
- Modify: `src/shared/types.ts`(TaskType += `'pipeline'`)
- Create: `src/main/services/pipeline/types.ts`、`validate.ts`、`store.ts`
- Test: `src/main/services/pipeline/__tests__/validate.spec.ts`、`store.spec.ts`

**Interfaces:**
- Produces: `PipelineStepType/PipelineStep/PipelineSchedule/PipelineRunState/Pipeline`、三类步骤参数接口、`validateSchedule/validateSteps`、`PipelineStore`(单例 `pipelineStore`)

- [ ] **Step 1: TaskType 扩展**

`src/shared/types.ts` 的 `TaskType` 联合类型末尾(`'text-timeline'` 之后)追加:

```ts
  | 'pipeline';            // 自动流水线(PRD-v2.1)
```

- [ ] **Step 2: 写失败测试**

`src/main/services/pipeline/__tests__/validate.spec.ts`:

```ts
/**
 * 流水线校验纯函数单测
 * 运行:node --test --import tsx src/main/services/pipeline/__tests__/validate.spec.ts
 */
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { validateSchedule, validateSteps } from '../validate';
import type { PipelineStep } from '../types';

describe('validateSchedule', () => {
  test('undefined 合法(不定时)', () => {
    assert.equal(validateSchedule(undefined), null);
  });

  test('at 非法 HH:mm 报错', () => {
    assert.ok(validateSchedule({ kind: 'daily', at: 'x' }));
    assert.ok(validateSchedule({ kind: 'daily', at: '24:00' }));
    assert.ok(validateSchedule({ kind: 'daily', at: '08:60' }));
  });

  test('weekly 缺 weekday 报错;weekday 越界(7)报错', () => {
    assert.ok(validateSchedule({ kind: 'weekly', at: '08:00' }));
    assert.ok(validateSchedule({ kind: 'weekly', at: '08:00', weekday: 7 }));
    assert.ok(validateSchedule({ kind: 'weekly', at: '08:00', weekday: -1 }));
  });

  test('daily 08:00 合法返回 null', () => {
    assert.equal(validateSchedule({ kind: 'daily', at: '08:00' }), null);
    assert.equal(validateSchedule({ kind: 'weekly', at: '08:00', weekday: 1 }), null);
    assert.equal(validateSchedule({ kind: 'once', at: '20:30' }), null);
  });
});

describe('validateSteps', () => {
  test('空 steps 报错', () => {
    assert.ok(validateSteps([]));
  });

  test('material-split 缺字段报错,齐全则通过', () => {
    const ok: PipelineStep = {
      type: 'material-split',
      params: { files: ['F:/a.mp4'], segmentSec: 5, outputDir: 'F:/out' },
    };
    assert.equal(validateSteps([ok]), null);
    assert.ok(validateSteps([{ type: 'material-split', params: { files: [], segmentSec: 5, outputDir: 'x' } }]));
    assert.ok(validateSteps([{ type: 'material-split', params: { files: ['a'], segmentSec: 0, outputDir: 'x' } }]));
    assert.ok(validateSteps([{ type: 'material-split', params: { files: ['a'], segmentSec: 5, outputDir: '' } }]));
  });

  test('video-mix-random:mode/folderIds 校验', () => {
    const ok: PipelineStep = {
      type: 'video-mix-random',
      params: { params: { mode: 'random', folderIds: ['f1'], resolution: '1080p', keepOriginalQuality: false } },
    };
    assert.equal(validateSteps([ok]), null);
    assert.ok(validateSteps([{ type: 'video-mix-random', params: { params: { mode: 'bad', folderIds: ['f1'] } } }]));
    assert.ok(validateSteps([{ type: 'video-mix-random', params: { params: { mode: 'random', folderIds: [] } } }]));
  });

  test('auto-publish:platform 非法/缺 title 报错;videoPath 允许为空', () => {
    const ok: PipelineStep = {
      type: 'auto-publish',
      params: { params: { platform: 'douyin', videoPath: '', title: '标题' }, usePrevArtifact: true },
    };
    assert.equal(validateSteps([ok]), null);
    assert.ok(validateSteps([{ type: 'auto-publish', params: { params: { platform: 'xxx', videoPath: '', title: 't' } } }]));
    assert.ok(validateSteps([{ type: 'auto-publish', params: { params: { platform: 'douyin', videoPath: '', title: '' } } }]));
  });

  test('未知 type 报错', () => {
    assert.ok(validateSteps([{ type: 'nope', params: {} }]));
  });
});
```

`src/main/services/pipeline/__tests__/store.spec.ts`:

```ts
/**
 * PipelineStore 单测:upsert/list/setRun/remove + 注入持久化
 */
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { PipelineStore } from '../store';
import type { Pipeline } from '../types';

function mkPipeline(name: string): Pipeline {
  return {
    id: `pl-${name}`,
    name,
    steps: [],
    createdAt: '2026-01-01T00:00:00.000Z',
    updatedAt: '2026-01-01T00:00:00.000Z',
  };
}

test('upsert/get/list/remove 与重启恢复', () => {
  const saved: Pipeline[] = [];
  const store = new PipelineStore({ load: () => saved, persist: (l) => { saved.splice(0, saved.length, ...l); } });
  store.upsert(mkPipeline('a'));
  store.upsert(mkPipeline('b'));
  assert.equal(store.list().length, 2);
  assert.equal(store.get('pl-a')?.name, 'a');
  assert.equal(store.remove('pl-a'), true);
  assert.equal(store.remove('pl-a'), false);

  const store2 = new PipelineStore({ load: () => saved, persist: () => {} });
  assert.equal(store2.list().length, 1);
});

test('setRun 写入 lastRun 与 lastRunAt', () => {
  const store = new PipelineStore({ load: () => [], persist: () => {} });
  store.upsert(mkPipeline('a'));
  const ok = store.setRun('pl-a', {
    startedAt: '2026-01-01T00:00:00.000Z',
    status: 'done',
    stepStatuses: ['done'],
  });
  assert.equal(ok, true);
  assert.equal(store.get('pl-a')?.lastRun?.status, 'done');
  assert.ok(store.get('pl-a')?.lastRunAt);
});
```

- [ ] **Step 3: 运行确认失败**

Run: `node --test --import tsx src/main/services/pipeline/__tests__/validate.spec.ts`
Expected: FAIL(模块不存在)

- [ ] **Step 4: 实现**

`src/main/services/pipeline/types.ts`:

```ts
/**
 * 自动流水线服务 - 类型定义(PRD-v2.1 FR-2/3)
 * 职责:管线、步骤、定时、运行态结构与三类全自动步骤参数
 */
import type { MixParams } from '../video-mix/types';
import type { PublishParams } from '../auto-publish/types';

/** 全自动步骤类型池(人工步骤不进链,完成后由 UI 给"建议下一步") */
export type PipelineStepType = 'material-split' | 'video-mix-random' | 'auto-publish';

/** 步骤运行状态 */
export type StepRunStatus = 'pending' | 'running' | 'done' | 'failed' | 'blocked' | 'cancelled';

/** 单个步骤 */
export interface PipelineStep {
  type: PipelineStepType;
  params: Record<string, unknown>;
}

/** 定时配置 */
export interface PipelineSchedule {
  kind: 'daily' | 'weekly' | 'once';
  /** 'HH:mm' */
  at: string;
  /** 0-6(0=周日),weekly 必填 */
  weekday?: number;
}

/** 管线运行状态(持久化到 lastRun) */
export interface PipelineRunState {
  startedAt: string;
  finishedAt?: string;
  status: 'running' | 'done' | 'failed' | 'cancelled';
  stepStatuses: StepRunStatus[];
  stepErrors?: (string | undefined)[];
  /** 每步产物(文件/目录),与 steps 一一对应 */
  artifacts?: (string | null)[];
  error?: string;
}

/** 管线 */
export interface Pipeline {
  id: string;
  name: string;
  steps: PipelineStep[];
  schedule?: PipelineSchedule;
  scheduleEnabled?: boolean;
  lastRunAt?: string;
  lastRun?: PipelineRunState;
  createdAt: string;
  updatedAt: string;
}

/** 素材分割步骤参数 */
export interface MaterialSplitStepParams {
  /** 待分割视频文件绝对路径列表 */
  files: string[];
  /** 每段时长(秒) */
  segmentSec: number;
  /** 输出目录 */
  outputDir: string;
}

/** 随机混剪步骤参数 */
export interface VideoMixStepParams {
  params: MixParams;
}

/** 自动发布步骤参数(videoPath 为空时自动使用上一步产物) */
export interface AutoPublishStepParams {
  params: PublishParams;
  usePrevArtifact?: boolean;
}
```

(`PublishParams` 从 `../auto-publish/types` import。)

`src/main/services/pipeline/validate.ts`:

```ts
/**
 * 流水线校验纯函数(PRD-v2.1 FR-2)
 * 职责:校验 schedule 与 steps(逐字段,返回首个错误,不抛错)
 */
import type {
  AutoPublishStepParams,
  MaterialSplitStepParams,
  PipelineSchedule,
  PipelineStep,
  VideoMixStepParams,
} from './types';

/** 平台枚举(与 auto-publish adapters 一致) */
const PLATFORMS = ['douyin', 'kuaishou', 'xiaohongshu', 'bilibili', 'shipinhao', 'spzx'];

/** 校验定时配置;undefined=不定时,合法 */
export function validateSchedule(s: PipelineSchedule | undefined): string | null {
  if (!s) return null;
  if (s.kind !== 'daily' && s.kind !== 'weekly' && s.kind !== 'once') return 'schedule.kind 非法';
  if (!/^\d{2}:\d{2}$/.test(s.at)) return 'schedule.at 需为 HH:mm';
  const [hh, mm] = s.at.split(':').map(Number);
  if (hh > 23 || mm > 59) return 'schedule.at 时间越界';
  if (s.kind === 'weekly' && (s.weekday === undefined || s.weekday < 0 || s.weekday > 6)) {
    return 'weekly 需要合法 weekday(0-6)';
  }
  return null;
}

/** 校验步骤数组;全部合法返回 null */
export function validateSteps(steps: PipelineStep[]): string | null {
  if (!Array.isArray(steps) || steps.length === 0) return 'steps 不能为空';
  for (let i = 0; i < steps.length; i++) {
    const step = steps[i];
    const tag = `steps[${i}]`;
    if (step.type === 'material-split') {
      const p = step.params as MaterialSplitStepParams;
      if (!Array.isArray(p.files) || p.files.length === 0) return `${tag}:files 不能为空`;
      if (!(p.segmentSec > 0)) return `${tag}:segmentSec 必须 > 0`;
      if (!p.outputDir) return `${tag}:outputDir 不能为空`;
    } else if (step.type === 'video-mix-random') {
      const p = (step.params as VideoMixStepParams).params;
      if (!p || (p.mode !== 'random' && p.mode !== 'audio-match')) return `${tag}:mode 非法`;
      if (!Array.isArray(p.folderIds) || p.folderIds.length === 0) return `${tag}:folderIds 不能为空`;
    } else if (step.type === 'auto-publish') {
      const p = (step.params as AutoPublishStepParams).params;
      if (!p) return `${tag}:params 缺失`;
      if (!PLATFORMS.includes(p.platform)) return `${tag}:platform 非法`;
      if (!p.title) return `${tag}:title 不能为空`;
      // videoPath 允许为空:可自动接上一步产物
    } else {
      return `${tag}:type 非法(${step.type})`;
    }
  }
  return null;
}
```

`src/main/services/pipeline/store.ts`(结构照 `schedule-store.ts`):

```ts
/**
 * 流水线存储(PRD-v2.1 FR-2)
 * 职责:Pipeline 持久化(userData/pipelines/pipelines.json),按 id 索引,变更即落盘
 */
import { app } from 'electron';
import { join, dirname } from 'path';
import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'fs';
import { logger } from '../../utils/logger';
import type { Pipeline, PipelineRunState } from './types';

export interface PipelineStoreDeps {
  load?: () => Pipeline[];
  persist?: (list: Pipeline[]) => void;
}

function defaultLoad(): Pipeline[] {
  try {
    const fp = join(app.getPath('userData'), 'pipelines', 'pipelines.json');
    if (!existsSync(fp)) return [];
    return JSON.parse(readFileSync(fp, 'utf8')) as Pipeline[];
  } catch (err) {
    logger.error(`[pipeline-store] 加载失败: ${err instanceof Error ? err.message : String(err)}`);
    return [];
  }
}

function defaultPersist(list: Pipeline[]): void {
  try {
    const fp = join(app.getPath('userData'), 'pipelines', 'pipelines.json');
    mkdirSync(dirname(fp), { recursive: true });
    writeFileSync(fp, JSON.stringify(list, null, 2), 'utf8');
  } catch (err) {
    logger.error(`[pipeline-store] 持久化失败: ${err instanceof Error ? err.message : String(err)}`);
  }
}

export class PipelineStore {
  private items = new Map<string, Pipeline>();
  private readonly loadFn: () => Pipeline[];
  private readonly persistFn: (list: Pipeline[]) => void;
  private loaded = false;

  constructor(deps: PipelineStoreDeps = {}) {
    this.loadFn = deps.load ?? defaultLoad;
    this.persistFn = deps.persist ?? defaultPersist;
  }

  private ensureLoaded(): void {
    if (this.loaded) return;
    for (const p of this.loadFn()) this.items.set(p.id, p);
    this.loaded = true;
  }

  upsert(p: Pipeline): void {
    this.ensureLoaded();
    this.items.set(p.id, p);
    this.persistFn(this.list());
  }

  get(id: string): Pipeline | null {
    this.ensureLoaded();
    return this.items.get(id) ?? null;
  }

  list(): Pipeline[] {
    this.ensureLoaded();
    return [...this.items.values()].sort((a, b) => b.updatedAt.localeCompare(a.updatedAt));
  }

  remove(id: string): boolean {
    this.ensureLoaded();
    const ok = this.items.delete(id);
    if (ok) this.persistFn(this.list());
    return ok;
  }

  /** 写入最近一次运行状态 */
  setRun(id: string, run: PipelineRunState): boolean {
    this.ensureLoaded();
    const p = this.items.get(id);
    if (!p) return false;
    p.lastRun = run;
    p.lastRunAt = run.startedAt;
    this.persistFn(this.list());
    return true;
  }
}

/** 全局单例 */
export const pipelineStore = new PipelineStore();
```

- [ ] **Step 5: 运行确认通过 + 提交**

Run: `node --test --import tsx src/main/services/pipeline/__tests__/validate.spec.ts src/main/services/pipeline/__tests__/store.spec.ts`
Expected: PASS

```powershell
git add src/shared/types.ts src/main/services/pipeline
git commit -m "feat(pipeline): 管线类型/校验纯函数/持久化存储(PRD-v2.1 FR-2)"
```

---

### Task 5: pipeline-runner 串行编排

**Files:**
- Create: `src/main/services/pipeline/runner.ts`
- Test: `src/main/services/pipeline/__tests__/runner.spec.ts`

**Interfaces:**
- Consumes: Task 4 的 `PipelineStep/PipelineRunState`
- Produces: `StepContext{prevArtifact,token,onStepProgress,taskId}`、`StepExecutor`、`runPipeline(steps, deps, token): Promise<PipelineRunState>`

- [ ] **Step 1: 写失败测试**

`src/main/services/pipeline/__tests__/runner.spec.ts`:

```ts
/**
 * 流水线运行器单测:串行、产物链、失败即停、取消、进度
 * 运行:node --test --import tsx src/main/services/pipeline/__tests__/runner.spec.ts
 */
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { runPipeline } from '../runner';
import type { PipelineStep } from '../types';
import { CancelToken } from '../../ffmpeg/types';

function steps(n: number): PipelineStep[] {
  return Array.from({ length: n }, () => ({ type: 'video-mix-random', params: {} }));
}

test('串行执行并链式传递产物', async () => {
  const seen: (string | null)[] = [];
  const exec = async (_step: PipelineStep, ctx) => {
    seen.push(ctx.prevArtifact);
    return `out-${seen.length}`;
  };
  const run = await runPipeline(steps(3), { executors: { 'video-mix-random': exec } }, new CancelToken('t'));
  assert.equal(run.status, 'done');
  assert.deepEqual(seen, [null, 'out-1', 'out-2']);
  assert.deepEqual(run.stepStatuses, ['done', 'done', 'done']);
  assert.deepEqual(run.artifacts, ['out-1', 'out-2', 'out-3']);
});

test('第 2 步失败:标记 failed + 后续 blocked,返回 failed', async () => {
  const exec = async (_step: PipelineStep, ctx: { prevArtifact: string | null }) => {
    if (ctx.prevArtifact === null) return 'artifact-1';
    throw new Error('boom');
  };
  const run = await runPipeline(steps(3), { executors: { 'video-mix-random': exec } }, new CancelToken('t'));
  assert.equal(run.status, 'failed');
  assert.deepEqual(run.stepStatuses, ['done', 'failed', 'blocked']);
  assert.equal(run.error, run.stepErrors?.[1]);
  assert.ok(run.error?.includes('boom'));
});

test('整体进度回调最终到 100', async () => {
  const exec = async () => 'out';
  const progressList: number[] = [];
  await runPipeline(
    steps(2),
    { executors: { 'video-mix-random': exec }, onProgress: (p) => progressList.push(p) },
    new CancelToken('t'),
  );
  assert.equal(progressList[progressList.length - 1], 100);
});

test('token 取消:当前步 cancelled,后续 blocked,状态 cancelled', async () => {
  const token = new CancelToken('t');
  const exec = async (_s: PipelineStep, ctx) => {
    token.cancel('用户取消');
    ctx.onStepProgress(10);
    throw new Error('CANCELLED');
  };
  const run = await runPipeline(steps(2), { executors: { 'video-mix-random': exec } }, token);
  assert.equal(run.status, 'cancelled');
  assert.deepEqual(run.stepStatuses, ['cancelled', 'blocked']);
});

test('onRunUpdate 逐步通知(持久化钩子)', async () => {
  const runs: string[] = [];
  const exec = async () => 'o';
  await runPipeline(steps(2), {
    executors: { 'video-mix-random': exec },
    onRunUpdate: (r) => runs.push(r.status),
  });
  // 每步至少通知一次 running,最终以终态结束
  assert.ok(runs.length >= 2);
  assert.equal(runs[runs.length - 1], 'done');
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test --import tsx src/main/services/pipeline/__tests__/runner.spec.ts`
Expected: FAIL

- [ ] **Step 3: 实现 runner.ts**

```ts
/**
 * 流水线运行器(PRD-v2.1 FR-2)
 * 职责:按 steps 串行执行注入的 StepExecutor,产物链式传递
 *       任一步失败即停,后续标 blocked;支持取消(标记 cancelled)
 * 设计要点:
 *  - 纯编排:执行器、进度回调、状态回调全部注入,可 node:test 单测
 *  - 步骤失败把错误写进 run.stepErrors[i],run.error 为首个失败信息
 */
import { CancelToken, FFmpegError } from '../ffmpeg/types';
import type { PipelineRunState, PipelineStep, PipelineStepType, StepRunStatus } from './types';

/** 步骤执行上下文 */
export interface StepContext {
  /** 关联的流水线任务 ID(task-queue) */
  taskId: string;
  /** 上一步产物;首步为 null */
  prevArtifact: string | null;
  /** 取消令牌(与流水线任务一致) */
  token: CancelToken;
  /** 步骤内进度(0-100,外层按步数折算) */
  onStepProgress: (p: number) => void;
}

/** 步骤执行器:返回产物路径(文件/目录);无产物返回 null */
export type StepExecutor = (step: PipelineStep, ctx: StepContext) => Promise<string | null>;

/** 执行器映射(按步骤类型) */
export type StepExecutors = Partial<Record<PipelineStepType, StepExecutor>>;

export interface RunPipelineDeps {
  executors: StepExecutors;
  /** 整体进度(0-100) */
  onProgress?: (p: number) => void;
  /** 运行状态变化钩子(持久化用) */
  onRunUpdate?: (run: PipelineRunState) => void;
}

/**
 * 串行执行流水线步骤
 * @returns 最终运行状态(含每步状态/错误/产物)
 */
export async function runPipeline(
  steps: PipelineStep[],
  deps: RunPipelineDeps,
  token: CancelToken,
  taskId: string = 'pipeline',
): Promise<PipelineRunState> {
  const run: PipelineRunState = {
    startedAt: new Date().toISOString(),
    status: 'running',
    stepStatuses: steps.map(() => 'pending' as StepRunStatus),
    stepErrors: steps.map(() => undefined),
    artifacts: steps.map(() => null),
  };
  const emit = () => deps.onRunUpdate?.(run);

  let prev: string | null = null;
  let cancelled = false;
  let failed = false;

  for (let i = 0; i < steps.length; i++) {
    if (failed) {
      run.stepStatuses[i] = 'blocked';
      continue;
    }
    if (token.isCancelled()) {
      run.stepStatuses[i] = 'blocked';
      run.status = 'cancelled';
      continue;
    }
    const exec = deps.executors[steps[i].type];
    if (!exec) {
      failed = true;
      run.stepStatuses[i] = 'failed';
      run.stepErrors![i] = `无 ${steps[i].type} 执行器`;
      for (let j = i + 1; j < steps.length; j++) run.stepStatuses[j] = 'blocked';
      run.error = `步骤 ${i + 1} 无可用执行器`;
      run.status = 'failed';
      break;
    }
    run.stepStatuses[i] = 'running';
    emit();
    try {
      const artifact = await exec(steps[i], {
        taskId,
        prevArtifact: prev,
        token,
        onStepProgress: (p) => {
          deps.onProgress?.(Math.round(((i + p) / steps.length) * 100));
        },
      });
      run.artifacts![i] = artifact;
      prev = artifact;
      run.stepStatuses[i] = 'done';
      deps.onProgress?.(Math.round(((i + 1) / steps.length) * 100));
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      run.stepErrors![i] = msg;
      const isCancelled = (err instanceof FFmpegError && err.code === 'CANCELLED') || token.isCancelled();
      if (isCancelled) {
        run.stepStatuses[i] = 'cancelled';
        run.status = 'cancelled';
        for (let j = i + 1; j < steps.length; j++) run.stepStatuses[j] = 'blocked';
        emit();
        return run;
      }
      failed = true;
      run.stepStatuses[i] = 'failed';
      run.stepErrors![i] = msg;
      for (let j = i + 1; j < steps.length; j++) run.stepStatuses[j] = 'blocked';
      run.status = 'failed';
      run.error = `步骤 ${i + 1} 失败: ${msg}`;
      emit();
      return run;
    }
    emit();
  }

  if (run.status === 'running') run.status = 'done';
  run.finishedAt = new Date().toISOString();
  emit();
  return run;
}
```

注:`onRunUpdate` 触发次数为"每步开始/结束各一次 + 最终一次",断言只要求 `>= 2` 且末次为终态,避免与实现细节强耦合。

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `node --test --import tsx src/main/services/pipeline/__tests__/runner.spec.ts`
Expected: PASS

```powershell
git add src/main/services/pipeline
git commit -m "feat(pipeline): runPipeline 串行编排(产物链/失败即停/取消)"
```

---

### Task 6: 定时调度(isPipelineDue + PipelineScheduler)

**Files:**
- Create: `src/main/services/pipeline/scheduler.ts`
- Test: `src/main/services/pipeline/__tests__/scheduler.spec.ts`

**Interfaces:**
- Consumes: Task 4 的 `Pipeline/PipelineSchedule/PipelineStore`
- Produces: `isPipelineDue(schedule, lastRunAt, now): boolean`;`PipelineScheduler`(`start/stop/runOnce`,依赖注入)

- [ ] **Step 1: 写失败测试**

```ts
/**
 * 流水线定时调度单测
 * 运行:node --test --import tsx src/main/services/pipeline/__tests__/scheduler.spec.ts
 */
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { isPipelineDue, PipelineScheduler } from '../scheduler';
import type { Pipeline, PipelineSchedule } from '../types';

const daily08: PipelineSchedule = { kind: 'daily', at: '08:00' };

describe('isPipelineDue', () => {
  test('daily:当天 08:00 后且今天未跑过 → due', () => {
    // 2026-01-05T09:00:00Z(本地时间按测试机时区,用本地构造)
    const now = new Date(2026, 0, 5, 9, 0).getTime();
    assert.equal(isPipelineDue(daily08, undefined, now), true);
    assert.equal(isPipelineDue(daily08, new Date(2026, 0, 5, 7, 0).toISOString(), now), true);
    assert.equal(isPipelineDue(daily08, new Date(2026, 0, 5, 8, 30).toISOString(), now), false);
  });

  test('weekly:weekday 不匹配不触发', () => {
    const s: PipelineSchedule = { kind: 'weekly', at: '08:00', weekday: 1 };
    const now = new Date(2026, 0, 5, 9, 0).getTime(); // 周一
    assert.equal(isPipelineDue(s, undefined, now), true);
    assert.equal(isPipelineDue({ ...s, weekday: 2 }, undefined, now), false);
  });

  test('once:从未跑过才触发', () => {
    const s: PipelineSchedule = { kind: 'once', at: '08:00' };
    const now = new Date(2026, 0, 5, 9, 0).getTime();
    assert.equal(isPipelineDue(s, undefined, now), true);
    assert.equal(isPipelineDue(s, new Date(2026, 0, 5, 8, 30).toISOString(), now), false);
  });

  test('非法 at 永不触发', () => {
    assert.equal(isPipelineDue({ kind: 'daily', at: 'xx' }, undefined, Date.now()), false);
  });
});

describe('PipelineScheduler', () => {
  test('runOnce 只触发到期且启用的管线', async () => {
    const fired: string[] = [];
    const mk = (id: string, schedule?: PipelineSchedule, scheduleEnabled?: boolean): Pipeline => ({
      id, name: id, steps: [], schedule, scheduleEnabled,
      createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(),
    });
    const pipelines = [mk('p1', daily08, true), mk('p2', daily08, false), mk('p3')];
    const scheduler = new PipelineScheduler({
      store: { list: () => pipelines, setRun: () => true },
      runner: (p) => {
        fired.push(p.id);
        return Promise.resolve();
      },
      checkIntervalMs: 1,
    });
    const ran = await scheduler.runOnce(new Date(2026, 0, 5, 9, 0).getTime());
    assert.equal(ran, 1);
    assert.deepEqual(fired, ['p1']);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test --import tsx src/main/services/pipeline/__tests__/scheduler.spec.ts`
Expected: FAIL

- [ ] **Step 3: 实现 scheduler.ts**

```ts
/**
 * 流水线定时调度(PRD-v2.1 FR-3)
 * 职责:isPipelineDue 纯函数判定到期;PipelineScheduler 轮询触发到点管线
 * 设计要点:
 *  - 到期判定纯函数可测;调度器只做"到期 → 入队执行"编排,失败静默记日志
 *  - 轮询间隔默认 60s;runner 注入(默认 wiring 见 index.ts)
 */
import { logger } from '../../utils/logger';
import type { Pipeline, PipelineRunState, PipelineSchedule } from './types';

/**
 * 判定管线是否到期应触发
 * @param schedule 定时配置
 * @param lastRunAt 最近运行时间(ISO,undefined=从未运行)
 * @param now 当前时间戳(毫秒)
 */
export function isPipelineDue(
  schedule: PipelineSchedule,
  lastRunAt: string | undefined,
  now: number,
): boolean {
  if (!/^\d{2}:\d{2}$/.test(schedule.at)) return false;
  const [hh, mm] = schedule.at.split(':').map(Number);
  if (hh > 23 || mm > 59) return false;
  const d = new Date(now);
  if (schedule.kind === 'weekly' && d.getDay() !== schedule.weekday) return false;
  const dueAt = new Date(d.getFullYear(), d.getMonth(), d.getDate(), hh, mm).getTime();
  if (now < dueAt) return false;
  if (schedule.kind === 'once' && lastRunAt) return false;
  // 今天已跑过(或上一轮触发点之后跑过)则不再触发
  if (!lastRunAt) return true;
  const last = new Date(lastRunAt).getTime();
  return last < dueAt;
}

/** PipelineScheduler 依赖注入 */
export interface PipelineSchedulerDeps {
  /** 流水线存储(需要 list/setRun) */
  store: { list: () => Pipeline[]; setRun: (id: string, run: PipelineRunState) => boolean };
  /** 触发执行回调(默认 wiring 由 index.ts 注入) */
  runner: (pipeline: Pipeline) => Promise<void>;
  /** 轮询间隔毫秒(默认 60s) */
  checkIntervalMs?: number;
}

/** 流水线定时调度器:固定间隔轮询到期条目 */
export class PipelineScheduler {
  private timer: NodeJS.Timeout | null = null;
  private readonly store: PipelineSchedulerDeps['store'];
  private readonly runner: PipelineSchedulerDeps['runner'];
  private readonly checkIntervalMs: number;
  private firing = false;

  constructor(deps: PipelineSchedulerDeps) {
    this.store = deps.store;
    this.runner = deps.runner;
    this.checkIntervalMs = deps.checkIntervalMs ?? 60 * 1000;
  }

  /** 启动轮询(幂等) */
  start(): void {
    if (this.timer) return;
    this.timer = setInterval(() => {
      void this.runOnce();
    }, this.checkIntervalMs);
    this.timer.unref?.();
  }

  /** 停止轮询 */
  stop(): void {
    if (this.timer) {
      clearInterval(this.timer);
      this.timer = null;
    }
  }

  /**
   * 执行一轮到期检查
   * @returns 触发的管线数
   */
  async runOnce(now: number = Date.now()): Promise<number> {
    let fired = 0;
    for (const p of this.store.list()) {
      if (!p.schedule || !p.scheduleEnabled) continue;
      if (!isPipelineDue(p.schedule, p.lastRunAt, now)) continue;
      try {
        await this.runner(p);
      } catch (err) {
        logger.warn(
          `[pipeline-scheduler] 触发 ${p.name} 失败: ${err instanceof Error ? err.message : String(err)}`,
        );
      }
      fired++;
    }
    return fired;
  }
}
```

- [ ] **Step 4: 运行确认通过 + 提交**

Run: `node --test --import tsx src/main/services/pipeline/__tests__/scheduler.spec.ts`
Expected: PASS

```powershell
git add src/main/services/pipeline
git commit -m "feat(pipeline): 定时调度 isPipelineDue + PipelineScheduler"
```

---

### Task 7: pipeline 服务 wiring + IPC

**Files:**
- Create: `src/main/services/pipeline/index.ts`、`src/main/ipc/pipeline.ipc.ts`
- Modify: `electron/ipc/index.ts`

**Interfaces:**
- Consumes: Task 4 的 store/validate、Task 5 的 runner、Task 6 的 isPipelineDue;`ffmpegService.split`、`videoMixService.runMix(params, taskId, token)`、`publishQueue.enqueue(PublishTask)`
- Produces: `pipelineService`(`save/list/get/remove/run/cancelRun`);6 通道 `pipeline:save/list/get/delete/run/cancelRun`

- [ ] **Step 1: 默认执行器 wiring**

`src/main/services/pipeline/index.ts`:

```ts
/**
 * 自动流水线服务单例(PRD-v2.1 FR-2/3)
 * 职责:组装默认步骤执行器(ffmpeg 分割/随机混剪/自动发布)+ 运行编排 + 定时调度启动
 */
import { taskQueue } from '../task-queue';
import type { TaskItem } from '../task-queue/types';
import { ffmpegService } from '../ffmpeg';
import { CancelToken } from '../ffmpeg/types';
import { videoMixService } from '../video-mix';
import { publishQueue } from '../auto-publish/publish-queue';
import type { PublishParams } from '../auto-publish/types';
import { logger } from '../../utils/logger';
import { pipelineStore } from './store';
import { runPipeline } from './runner';
import { isPipelineDue } from './scheduler';
import type {
  AutoPublishStepParams,
  MaterialSplitStepParams,
  Pipeline,
  PipelineRunState,
  VideoMixStepParams,
} from './types';

/** 活跃流水线任务的取消令牌:taskId → CancelToken */
const activeTokens = new Map<string, CancelToken>();

/** 素材分割执行器:逐文件调用 ffmpegService.split,产物为输出目录 */
async function runMaterialSplit(step, ctx) {
  const p = step.params as unknown as MaterialSplitStepParams;
  for (let i = 0; i < p.files.length; i++) {
    const segs = await ffmpegService.split(p.files[i], p.segmentSec, p.outputDir, {}, ctx.token);
    logger.info(`[pipeline] 分割 ${i + 1}/${p.files.length} 完成,产出 ${segs.length} 段`);
    ctx.onStepProgress(Math.round(((i + 1) / p.files.length) * 100));
  }
  return p.outputDir;
}

/** 随机混剪执行器:复用 videoMixService.runMix,产物为输出文件 */
async function runVideoMix(step, ctx) {
  const p = (step.params as VideoMixStepParams).params;
  const result = await videoMixService.runMix(
    { ...p, outputName: p.outputName || `pipeline-${Date.now()}.mp4` },
    ctx.taskId,
    ctx.token,
  );
  return result.outputPath;
}

/** 自动发布执行器:videoPath 为空时自动接上一步产物;入队后立即返回(执行由发布串行链接管) */
function runAutoPublish(step, ctx) {
  const p = step.params as unknown as AutoPublishStepParams;
  const videoPath = p.usePrevArtifact && ctx.prevArtifact ? ctx.prevArtifact : p.params.videoPath;
  const params: PublishParams = { ...p.params, videoPath };
  publishQueue.enqueue({
    id: `publish-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`,
    status: 'pending',
    progress: 0,
    createdAt: new Date().toISOString(),
    params,
  });
  return Promise.resolve(null);
}

/** 执行单条流水线:入队 taskQueue + 串行跑步骤 + 三结局处理 */
export async function executePipeline(pipeline: Pipeline): Promise<PipelineRunState> {
  const taskId = `pipeline-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
  const task: TaskItem = {
    id: taskId,
    type: 'pipeline',
    title: `流水线:${pipeline.name}`,
    status: 'pending',
    progress: 0,
    params: { pipelineId: pipeline.id } as Record<string, unknown>,
    createdAt: new Date().toISOString(),
  };
  taskQueue.enqueue(task);

  const token = new CancelToken(taskId);
  activeTokens.set(taskId, token);

  const run = await runPipeline(
    pipeline.steps,
    {
      executors: {
        'material-split': runMaterialSplit,
        'video-mix-random': runVideoMix,
        'auto-publish': runAutoPublish,
      },
      onProgress: (p) => taskQueue.updateProgress(taskId, p),
      onRunUpdate: (r) => pipelineStore.setRun(pipeline.id, r),
    },
    token,
    taskId,
  );

  activeTokens.delete(taskId);
  if (run.status === 'done') taskQueue.complete(taskId);
  else taskQueue.fail(taskId, run.error ?? `流水线${run.status}`);

  pipelineStore.setRun(pipeline.id, run);
  return run;
}

/** 手动取消运行中的流水线任务 */
export function cancelPipelineRun(taskId: string): void {
  const token = activeTokens.get(taskId);
  if (token) {
    token.cancel('用户取消流水线');
    activeTokens.delete(taskId);
  }
  taskQueue.cancel(taskId);
}

/** 流水线定时调度器(轮询到期并执行) */
export class PipelineSchedulerService {
  private timer: NodeJS.Timeout | null = null;

  start(checkIntervalMs = 60 * 1000): void {
    if (this.timer) return;
    this.timer = setInterval(() => {
      const now = Date.now();
      for (const p of pipelineStore.list()) {
        if (!p.schedule || !p.scheduleEnabled) continue;
        if (!isPipelineDue(p.schedule, p.lastRunAt, now)) continue;
        void executePipeline(p).catch((err) =>
          logger.error(`[pipeline] 定时执行失败: ${err instanceof Error ? err.message : String(err)}`),
        );
      }
    }, checkIntervalMs);
    this.timer.unref?.();
  }
}

export const pipelineScheduler = new PipelineSchedulerService();
```

(实现时:函数签名补上来自 types 的类型标注,`runMaterialSplit(step: PipelineStep, ctx: StepContext)` 等,满足 electron tsc 严格模式;`executePipeline` 里 `runPipeline` 的参数顺序按 Task 4 导出为准。)

- [ ] **Step 2: IPC 模块**

`src/main/ipc/pipeline.ipc.ts`:

```ts
/**
 * 自动流水线 IPC 注册(PRD-v2.1 FR-2)
 * 通道:pipeline:save / list / get / delete / run / cancelRun
 */
import type { ipcMain } from 'electron';
import { safeHandle } from '../../../electron/ipc/index';
import { pipelineStore } from '../services/pipeline/store';
import { validateSchedule, validateSteps } from '../services/pipeline/validate';
import { executePipeline, pipelineScheduler } from '../services/pipeline/index';
import type { Pipeline } from '../services/pipeline/types';

export function register(ipc: typeof ipcMain): void {
  safeHandle(ipc, 'pipeline:save', async (_event, payload) => {
    const p = payload as Partial<Pipeline> | undefined;
    if (!p?.name || typeof p.name !== 'string') throw new Error('pipeline:save 缺少 name');
    const err = validateSteps(p.steps ?? []);
    if (err) throw new Error(`pipeline:save 参数无效:${err}`);
    const scheduleErr = validateSchedule(p.schedule);
    if (scheduleErr) throw new Error(`pipeline:save 参数无效:${scheduleErr}`);
    const now = new Date().toISOString();
    const existing = p.id ? pipelineStore.get(p.id) : null;
    const pipeline: Pipeline = {
      id: existing?.id ?? `pl-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`,
      name: p.name,
      steps: p.steps!,
      schedule: p.schedule,
      scheduleEnabled: p.scheduleEnabled ?? false,
      lastRunAt: existing?.lastRunAt,
      lastRun: existing?.lastRun,
      createdAt: existing?.createdAt ?? now,
      updatedAt: now,
    };
    pipelineStore.upsert(pipeline);
    return pipeline;
  });

  safeHandle(ipc, 'pipeline:list', async () => pipelineStore.list());

  safeHandle(ipc, 'pipeline:get', async (_event, payload) => {
    const { id } = payload as { id: string };
    if (!id) throw new Error('pipeline:get 缺少 id');
    return pipelineStore.get(id);
  });

  safeHandle(ipc, 'pipeline:delete', async (_event, payload) => {
    const { id } = payload as { id: string };
    if (!id) throw new Error('pipeline:delete 缺少 id');
    return pipelineStore.remove(id);
  });

  safeHandle(ipc, 'pipeline:run', async (_event, payload) => {
    const { id } = payload as { id: string };
    if (!id) throw new Error('pipeline:run 缺少 id');
    const pipeline = pipelineStore.get(id);
    if (!pipeline) throw new Error(`pipeline:run 流水线不存在: ${id}`);
    void executePipeline(pipeline).catch(() => {
      /* 三结局已在 executePipeline 内处理,这里吞掉异步异常避免未捕获 */
    });
    return { started: id };
  });

  safeHandle(ipc, 'pipeline:startScheduler', async () => {
    pipelineScheduler.start();
    return { started: true };
  });
}
```

- [ ] **Step 3: 注册**

`electron/ipc/index.ts` 同 Task 2 模式追加 `registerPipeline`,并在 `register` 函数末尾(启动期)调用一次 `pipelineScheduler.start()`——改为在 `pipeline.ipc.ts` 的 register 尾部 `try { pipelineScheduler.start(); } catch {}`(照 auto-publish.ipc.ts 494-503 行的容错模式)。

- [ ] **Step 4: 门禁 + 提交**

Run: `npm test` → PASS;`npx tsc -p tsconfig.electron.json --noEmit` → PASS

```powershell
git add src/main/services/pipeline src/main/ipc/pipeline.ipc.ts electron/ipc/index.ts
git commit -m "feat(pipeline): 服务单例 + IPC 通道 + 默认执行器接线"
```

---

### Task 8: PipelineView + 路由 + 侧边栏

**Files:**
- Create: `src/renderer/views/pipeline/PipelineView.vue`
- Modify: `src/renderer/router/index.ts`、`src/renderer/components/layout/Sidebar.vue`

- [ ] **Step 1: 路由**

`src/renderer/router/index.ts` 在 `/hot-topics` 条目后追加:

```ts
  {
    path: '/pipeline',
    name: 'pipeline',
    component: () => import('../views/pipeline/PipelineView.vue'),
  },
```

- [ ] **Step 2: PipelineView.vue**

视图结构(完整实现,步骤编辑器 + 定时 + 运行):

```vue
<script setup lang="ts">
/**
 * 自动流水线视图(PRD-v2.1 FR-2/3)
 * 职责:管线列表 + 步骤编排(增删/上移下移/参数表单)+ 定时设置 + 运行
 */
import { ref, onMounted } from 'vue';
import type { Pipeline, PipelineSchedule, PipelineStep, PipelineStepType } from '@main/services/pipeline/types';

const pipelines = ref<Pipeline[]>([]);
const editing = ref<Pipeline | null>(null);
const message = ref('');

function getApi() {
  return (window as unknown as { api: { invoke: <TReq, TResp>(c: string, p?: unknown) => Promise<{ ok: boolean; data?: TResp; error?: string }> } }).api;
}

async function refresh(): Promise<void> {
  const res = await getApi().invoke<undefined, Pipeline[]>('pipeline:list');
  if (res.ok && res.data) pipelines.value = res.data;
}
onMounted(refresh);

function newStep(type: PipelineStepType): PipelineStep {
  if (type === 'material-split') {
    return { type, params: { files: [], segmentSec: 5, outputDir: '' } };
  }
  if (type === 'video-mix-random') {
    return { type, params: { params: { mode: 'random', folderIds: [], perFolderCount: 3, resolution: '1080p', keepOriginalQuality: false } } };
  }
  return { type: 'auto-publish', params: { params: { platform: 'douyin', videoPath: '', title: '' }, usePrevArtifact: true } };
}

function startNew(): void {
  editing.value = { id: '', name: '', steps: [], scheduleEnabled: false, createdAt: '', updatedAt: '' };
}

function edit(p: Pipeline): void {
  editing.value = JSON.parse(JSON.stringify(p));
}

function addStep(): void {
  if (!editing.value) return;
  editing.value.steps.push({ type: 'material-split', params: { files: [], segmentSec: 5, outputDir: '' } });
}

function removeStep(i: number): void {
  editing.value?.steps.splice(i, 1);
}

function moveStep(i: number, dir: -1 | 1): void {
  const steps = editing.value?.steps;
  if (!steps) return;
  const j = i + dir;
  if (j < 0 || j >= steps.length) return;
  [steps[i], steps[j]] = [steps[j], steps[i]];
}

async function save(): Promise<void> {
  if (!editing.value) return;
  const payload = JSON.parse(JSON.stringify(editing.value));
  const res = await getApi().invoke<Partial<Pipeline>, Pipeline>('pipeline:save', payload);
  if (!res.ok) {
    message.value = res.error ?? '保存失败';
    return;
  }
  message.value = '已保存';
  editing.value = null;
  await refresh();
}

async function run(p: Pipeline): Promise<void> {
  await getApi().invoke<{ id: string }, unknown>('pipeline:run', { id: p.id });
  message.value = `流水线「${p.name}」已开始,进度见任务中心`;
}

async function remove(p: Pipeline): Promise<void> {
  await getApi().invoke<{ id: string }, boolean>('pipeline:delete', { id: p.id });
  await refresh();
}

/** JSON 编辑步骤参数(M1 简化:高级参数用 JSON 文本域,平台下拉等常用项用表单) */
function paramsJson(step: PipelineStep): string {
  return JSON.stringify(step.params, null, 2);
}
function onParamsInput(step: PipelineStep, ev: Event): void {
  const target = ev.target as HTMLTextAreaElement;
  try {
    step.params = JSON.parse(target.value);
    message.value = '';
  } catch {
    message.value = '步骤参数 JSON 非法(暂存,保存时校验)';
  }
}

async function toggleSchedule(p: Pipeline): Promise<void> {
  p.scheduleEnabled = !p.scheduleEnabled;
  await getApi().invoke<Partial<Pipeline>, Pipeline>('pipeline:save', JSON.parse(JSON.stringify(p)));
  await refresh();
}
</script>

<template>
  <div class="pipeline-view">
    <header>
      <h2>自动流水线</h2>
      <button class="btn primary" @click="startNew">新建流水线</button>
    </header>
    <p v-if="message" class="msg">{{ message }}</p>

    <!-- 编辑面板 -->
    <section v-if="editing" class="editor">
      <input v-model="editing.name" placeholder="流水线名称" />
      <div v-for="(step, i) in editing.steps" :key="i" class="step-card">
        <div class="step-head">
          <span>{{ i + 1 }}. {{ step.type }}</span>
          <button @click="moveStep(i, -1)">↑</button>
          <button @click="moveStep(i, 1)">↓</button>
          <button @click="removeStep(i)">✕</button>
        </div>
        <textarea :value="paramsJson(step)" rows="5" @change="onParamsInput(step, $event)"></textarea>
      </div>
      <div class="step-add">
        <button @click="addStep">+ 素材分割</button>
        <button @click="editing.steps.push({ type: 'video-mix-random', params: { params: { mode: 'random', folderIds: [], perFolderCount: 3, resolution: '1080p', keepOriginalQuality: false } } })">+ 随机混剪</button>
        <button @click="editing.steps.push({ type: 'auto-publish', params: { params: { platform: 'douyin', videoPath: '', title: '' }, usePrevArtifact: true } })">+ 自动发布</button>
      </div>
      <div class="schedule-row">
        <label><input v-model="editing.scheduleEnabled" type="checkbox" /> 启用定时</label>
        <select v-if="editing.schedule" v-model="editing.schedule.kind">
          <option value="daily">每天</option>
          <option value="weekly">每周</option>
          <option value="once">一次</option>
        </select>
        <input v-if="editing.schedule" v-model="editing.schedule.at" placeholder="08:00" />
        <select v-if="editing.schedule?.kind === 'weekly'" v-model.number="editing.schedule.weekday">
          <option v-for="(n, i) in ['周日','周一','周二','周三','周四','周五','周六']" :key="i" :value="i">{{ n }}</option>
        </select>
        <button v-if="!editing.schedule" @click="editing.schedule = { kind: 'daily', at: '08:00' }">设置定时</button>
      </div>
      <div class="editor-actions">
        <button class="btn primary" @click="save">保存</button>
        <button class="btn" @click="editing = null">取消</button>
      </div>
    </section>

    <!-- 列表 -->
    <section v-else>
      <button class="btn primary" @click="startNew">新建流水线</button>
      <div v-for="p in pipelines" :key="p.id" class="pipeline-card">
        <div class="card-head">
          <strong>{{ p.name }}</strong>
          <span>{{ p.steps.map((s) => s.type).join(' → ') }}</span>
        </div>
        <div class="card-actions">
          <button class="btn primary" @click="run(p)">运行</button>
          <button @click="edit(p)">编辑</button>
          <button @click="toggleSchedule(p)">{{ p.scheduleEnabled ? '停用定时' : '启用定时' }}</button>
          <span v-if="p.schedule && p.scheduleEnabled">🕓 {{ p.schedule.kind }} {{ p.schedule.at }}</span>
          <span v-if="p.lastRun">最近:{{ p.lastRun.status }}</span>
        </div>
      </div>
    </section>
    <p class="hint">人工步骤(文本精修)不进链;完成后在任务中心取产物,再手动精修/发布。</p>
  </div>
</template>

<style scoped>
.pipeline-view { padding: 20px; }
.pipeline-card, .step-card, .editor { border: 1px solid #ddd; border-radius: 8px; padding: 12px; margin-bottom: 12px; }
.card-head, .step-head { display: flex; gap: 8px; align-items: center; justify-content: space-between; }
.btn.primary { background: #2563eb; color: #fff; }
.hint { color: #888; font-size: 12px; }
</style>
```

(以上模板中 `editing.steps.push(...)` 在模板里调用 `editing` 需 `.value` 由模板自动解包——`<script setup>` 模板里 ref 自动解包,`editing.steps.push(...)` 合法。)

- [ ] **Step 3: 侧边栏**

修改 `src/renderer/components/layout/Sidebar.vue`:先读该文件找到导航项数组(与 `text-timeline` 条目同构),追加:

```ts
{ path: '/pipeline', icon: '🔄', label: '自动流水线' }
```

(以文件中既有条目的真实字段名为准——若为 `title` �非 `label`,保持一致。)

- [ ] **Step 4: 门禁 + 提交**

Run: `npm run typecheck` → PASS;`npm test` → PASS

```powershell
git add src/renderer
git commit -m "feat(pipeline): 自动流水线视图与导航(PRD-v2.1 FR-2/3)"
```

---

### Task 9: 文档同步 + 全量门禁

**Files:**
- Modify: `docs/操作手册.md`(功能列表加"自动流水线/混剪参数模板"小节)
- Modify: `docs/全流程操作指南.md`(新增管线工作流步骤)
- Modify: `AGENTS.md`(IPC 通道数更新为 131+10=141,架构地图加 `mix-template/`、`pipeline/`)

- [ ] **Step 1: 操作手册**加两节:①「混剪参数模板」(随机混剪页存模板/套用);②「自动流水线」(新建管线→编排步骤→运行/定时;失败即停说明;产物在任务中心)。
- [ ] **Step 2: 全流程指南**加"管线 F:分割→混剪→发布一键跑"小节。
- [ ] **Step 3: AGENTS.md** 架构地图 `src/main/services/` 下补 `mix-template/`(任务级参数模板)与 `pipeline/`(流水线+定时);IPC 通道数同步。
- [ ] **Step 4: 门禁三连全绿**

```powershell
npm test
npm run typecheck
npx tsc -p tsconfig.electron.json --noEmit
```

- [ ] **Step 5: 提交推送双远端**

```powershell
git add docs/操作手册.md docs/全流程操作指南.md AGENTS.md
git commit -m "docs: v2.1 M1 自动流水线与参数模板文档"
git push github main; git push gitee main
```

---

## Self-Review 结果

1. **Spec coverage**:FR-1(Task 1-3)、FR-2(Task 4-8)、FR-3(Task 6-8 的 scheduler + View 定时 UI)全覆盖;M1 无遗漏。
2. **Placeholder scan**:无 TBD/占位断言;所有代码块可直接落盘。
3. **Type consistency**:`runPipeline(steps, deps, token, taskId)` 在 Task 5 定义、Task 7 使用一致;`pipelineStore.setRun(id, run)` 签名 Task 4 定义、Task 6 scheduler deps 一致;TaskType `'pipeline'` 在 Task 4 Step 1 加入,Task 7 executePipeline 使用;`StepExecutor(taskId, prevArtifact, token, onStepProgress)` 与三个默认执行器签名一致。
