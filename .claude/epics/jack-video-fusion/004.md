---
name: 素材仓库与文件夹隔离机制
status: open
created: 2026-07-30T10:36:07Z
updated: 2026-07-30T10:36:07Z
github: (will be set on sync)
depends_on: [002]
parallel: true
conflicts_with: [008]
---

# Task: 素材仓库与文件夹隔离机制

## Description
实现"以文件夹为作用域"的素材仓库,所有匹配 / 抽取算法强制同文件夹。代码层固化规则,杜绝跨文件夹调用。

## Acceptance Criteria
- [ ] `material-repo` 模块:文件夹注册、素材扫描(MP4/MOV/MP3/TXT/SRT)、元数据落库
- [ ] API 强制作用域:`pickFromFolder(folderId, count)` 不接受 `folderIds[]`
- [ ] 跨文件夹调用必须显式走 `pickAcrossFolders(folderIds[], whitelistPolicy)` 并打印审计日志
- [ ] 单元测试:`folderIsolation.spec.ts` 覆盖率 ≥ 95%,关键路径(混剪、音频匹配、TTS 配素材)验证 100% 隔离
- [ ] 拖拽导入 / 手动添加文件夹双通道

## Technical Details
- `(folderId, fileId)` 二元组为作用域单位
- 审计日志写入 `data/audit.log`
- 目录约定:`src/main/services/material-repo/`

## Dependencies
- 002(持久化与配置服务)

## Effort Estimate
- Size: M
- Hours: 8-10

## Definition of Done
- [ ] 文件夹隔离单测全部通过
- [ ] UI 拖拽体验流畅
- [ ] 审计日志可查询
- [ ] 代码含函数级注释
